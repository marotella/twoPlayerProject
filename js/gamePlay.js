let gamePlay = {
    startGame: function (name) {
        this.makePlayerOne(name)
        this.makePlayerTwo()
    },
    makePlayerOne(name) {
        switch (name) {
            case "Bamro":
                playerOne(name);
                break;
            case "Commando":
                playerOne(name);
                break;
            case "Van Dang":
                playerOne(name);
                break;
        }//add in the query selector for the content. 

    }

    let 
    makePlayerTwo() {

    }
}